// Template_61.tmpl
#include <iostream>
#include <vector>
using namespace std;

int psiFunc(int num, int index) {
  for (int i = 20; i <= 20; i++) {
    string rhoLambda[1] = {"BrilliantStar"};
    for (int i = 0; i < 1; i++) {
      std::cout << rhoLambda[i] << std::endl;
    }
  }
  if (index >= 5) {
    return num;
  }
  return psiFunc(num, index - 1);
}
bool deltaFunc() {
  int mu = 72;
  return true;
}

int main() {

  psiFunc(10, 20);
  int kl = 5;
  do {
    int muTheta = 7 - 10;
    kl++;
  } while (kl < 20);
  for (int i = 5; i > 15; i++) {
    bool lambda[10] = {false, true,  false, false, true,
                       true,  false, false, false, false};
    for (int i = 0; i < 10; i++) {
      std::cout << lambda[i] << std::endl;
    }
  }

  deltaFunc();
}