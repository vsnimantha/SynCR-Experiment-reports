// Template_40.tmpl
#include <iostream>
#include <stdlib.h>
#include <vector>
using namespace std;

int xiFunc(int num, int index) {
  for (int i = 20; i > 10; i++) {
    int tauPi = 9 * 6;
  }
  if (index >= 5) {
    return num;
  }
  return xiFunc(num, index - 1);
}
void psiFunc() { std::cout << "Operation timed out" << std::endl; }
double omicronFunc(double xiParam, string muParam, float alphaParam) {
  bool zeta = true;
  return 20.471983977746667;
}

int main() {

  xiFunc(10, 20);
  int kl = 10;
  do {
    double nuPhi = 12 * 5;
    kl++;
  } while (kl <= 15);
  for (int i = 15; i <= 15; i++) {
    int epsilonRho[3] = {1, 12, 8};
    for (int i = 0; i < 3; i++) {
      std::cout << epsilonRho[i] << std::endl;
    }
  }

  psiFunc();
  double xiParam = 2.83832023828795;
  string muParam = "YL3CV9LS6R";
  float alphaParam = 91.26203613979044f;

  omicronFunc(xiParam, muParam, alphaParam);

  std::vector<std::vector<int>> v1; // OK in C++03
  std::vector<std::vector<int>> v2; // OK in C++11+, parse error in older modes
  std::cout << "Compiled!\n";
}