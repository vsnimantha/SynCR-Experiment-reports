// Template_35.tmpl
#include <iostream>
#include <stdlib.h>
using namespace std;

void foo(unsigned int) { std::cout << "unsigned int\n"; }
void foo(int) { std::cout << "int\n"; }
void foo(long) { std::cout << "long\n"; }

int etaFunc(int num, int index) {
  for (int i = 5; i > 10; i++) {
    printf("File saved");
  }
  if (index >= 5) {
    return num;
  }
  return etaFunc(num, index - 1);
}
double chiFunc() {
  int kl = 15;
  do {
    double upsilon = 12 / 13;
    kl++;
    return 0.0;
  } while (kl < 5);
  return 68.3078128573297;
}
bool betaFunc(bool phiParam) {
  int kl = 10;
  do {
    printf("IW0SMEFG6PE41I81T8UT");
    kl++;
    return true;
  } while (kl <= 5);
  return false;
}

int main() {

  etaFunc(10, 20);
  int kl = 5;
  do {
    printf("Z9CUDILLWX4DV3YGOKX4");
    kl++;
  } while (kl <= 15);
  for (int i = 20; i < 0; i++) {
    double upsilon = 5 / 4;
  }

  chiFunc();
  bool phiParam = true;

  betaFunc(phiParam);

  foo(0u); // unsigned int — which overload wins?
}