// Template_56.tmpl
#include <iostream>
#include <stdexcept>
using namespace std;

int betaFunc(int num, int index) {
  std::cout << "Warning: low memory" << std::endl;
  if (index >= 5) {
    return num;
  }
  return betaFunc(num, index - 1);
}
bool zetaFunc() {
  std::cout << "This is a sample print statement" << std::endl;
  return true;
}

int main() {

  betaFunc(10, 20);
  int kl = 20;
  do {
    double xiRho = 4 / 7;
    kl++;
  } while (kl < 20);
  for (int i = 5; i <= 15; i++) {
    printf("ACJN87QS981GYUQHOB5O");
  }

  zetaFunc();

  throw 42; // throws int
  return 0;
}